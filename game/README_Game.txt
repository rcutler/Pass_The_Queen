To run the game client and view to mess around with locally without network code, do the following:
go run bughouse_chess.go

This will eventually be changed and all initialized once the client is started up. Will be done when integrated with the networking code.
